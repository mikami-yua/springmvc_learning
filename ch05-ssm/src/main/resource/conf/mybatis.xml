<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>

    <!--设置别名-->
    <typeAliases>
        <!--name:实体类所在的包名-->
        <package name="domain"/>
    </typeAliases>

    <!--sql映射文件的位置-->
    <mappers>
        <!--name：包名，这个包中的所有mapper.xml文件一次都能加载
        使用package的要求：
            1.mapper文件名称和dao接口必须完全一样
            2.mapper文件和dao接口必须在同一个目录
        -->
        <package name="dao"/>
    </mappers>
</configuration>
